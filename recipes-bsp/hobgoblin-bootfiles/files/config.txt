# Device Tree
# By default, the genesysII build uses the gigabit axi emac ip.
# However, it can also be configured to use the Ethernet Lite IP,
# which is license free although lower performance. Change to the
# etherlite dtb instead of the axi-emac in that case.
#/a70x-hobgoblin-etherlite.dtb	0x87000000 FDT
/a70x-hobgoblin-axi-emac.dtb	0x87000000 FDT

# OpenSBI
/fw_dynamic.bin		0x80000000 BOOT
# U-Boot
/u-boot.bin		0x80200000 NXT
# Linux
# /Image.gz		0x81000000
# Ramdisk
# /rootfs.img		0x82000000
